
# Dependency
OpenCV  
OpenMVG  
Boost

# Installation Guide

## Install OpenCV
	sudo apt-get install libopencv-dev
## Install OpenMVG
## Install Boost
	sudo apt-get install libboost-all-dev

# Build Instruction
	mkdir build 
	cd build
	cmake ..
	make
